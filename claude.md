# Claude Code 项目强制规范

## 1. 核心架构铁律（解决分类乱、样式进JS问题）
- **页面独立**：每个功能必须是一个独立的 `.html` 文件。严禁在 JavaScript 中使用 `innerHTML` 或模板字符串生成页面主体骨架。
- **样式内联**：所有样式必须写在当前页面 `<head>` 的 `<style>` 标签内。严禁使用外部 `.css` 文件链接。
- **JS 样式禁区**：JavaScript 绝对禁止操作 `style.xxx`、`cssText` 或任何样式字符串。JS 仅负责：数据请求、事件绑定、通过 `classList.add/remove` 切换类名。
- **分类标注**：每个 `<style>` 内的选择器必须用该页面独有的父级类名（如 `.page-home`）包裹，防止样式污染。

## 2. 修改安全协议（解决无备份、乱删东西）
- **预告机制**：修改代码前，必须先列出【修改预告】，用列表注明将要删除、重构或重命名的函数/变量/HTML区块。
- **完整输出**：禁止在代码中使用 `...`、`省略` 或 `// 此处保持不变`。过长可分批输出（如先输出 index.html），但每次必须给完整文件。
- **版本兜底**：整体重构时，必须输出带 `_v2` 后缀的新文件（如 `index_v2.html`），等我确认再建议覆盖。

## 3. 构建日志格式（解决敷衍日记）
- **强制表格**：每次回复的代码末尾，必须附带 Markdown 表格作为修改日志。
- **详细描述**：表格中“具体变更内容”每项描述必须超过 20 个汉字。
- **禁用词**：严禁使用“优化”、“完善”、“调整”等模糊词，必须写具体动作（如：将列表渲染从 `for` 循环改为 `map` 方法，并增加了空状态判断）。
- **待办风险**：表格末尾必须列出本次修改可能影响的区域或遗留问题。

## 4. 启动确认
在开始输出代码前，先回复“已确认规则，现在开始处理”。

## 5. 上下文恢复规则
每次新开需求或压缩上下文后，必须先完整读取 `README.md`（项目文档）和 `claude.md`（项目规范）这两个文件，确认当前项目结构、文件命名、数据字段和命名规范后，再开始修改代码。不得凭记忆或猜测直接开始修改。

## Git 自动备份规则

当用户说“提交到git”、“提交代码”、“commit”或“备份到git”时，自动执行以下流程：

1. **检查状态**：执行 `git status`，将修改文件列表展示给用户。
2. **生成 commit message**：根据本次对话中修改的内容，自动生成一条有意义的 commit message（概括主要变更，不少于 10 个字）。
3. **确认执行**：将生成的 message 展示给用户，并询问“是否确认提交？”。
4. **提交操作**：用户确认后，依次执行：
   - `git add .`
   - `git commit -m "生成的message"`
5. **推送（可选）**：如果用户额外说“推送”或“push”，则执行 `git push`。
6. **反馈结果**：将命令执行结果（成功或失败）返回给用户。

注意：所有 git 命令在执行前必须先让用户确认，严禁未经确认直接执行。


## 修改规则
当用户给指令时需分析此行为的利弊，询问用户喜欢哪种再进行修改