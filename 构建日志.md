# 简纸 Kanshi v3 — 构建日志

## 2026-07-20

### 字体修复 + 天气/运签重构

| 序号 | 文件 | 具体变更内容 | 影响区域 |
|------|------|-------------|----------|
| 1 | 全部 19 个 HTML | 将错误的 CSS 选择器 `.page-xxx body{` 修正为 `body.page-xxx{`（原写法永远匹配不上，导致字体、背景、边距全部没生效） | 所有页面的根样式 |
| 2 | 全部 19 个 HTML | Google Fonts 链接追加 `Inter` 字体族，新增 `.num{}` 工具类（等宽无衬线加粗，供数字/统计专用） | 字体加载 |
| 3 | index.html | 运签从 `Math.random()` 数组改为 `Lunar.fromDate().getDayInGanZhi()` 实时计算，绑定 60 甲子每日一首不重复 | 首页运签展示 |
| 4 | index.html | 运签签名诗库扩展到 60 首完整版本（甲子→癸亥），融入对应生肖意象 | 首页运签展示 |
| 5 | index.html | 幸运色从天干 5 色改为地支 12 色（中国传统色：玄黑/土黄/松绿/藕荷/缃色/朱砂/胭脂/荼白/月白/鎏金/驼色/黛紫） | 首页幸运色展示 |
| 6 | index.html | 天气从硬编码随机数组改为 `navigator.geolocation` 定位 + Open-Meteo 免费 API 获取实时温度/天气码 | 首页天气组件 |
| 7 | index.html | 天气城市名从硬编码改为 Open-Meteo 反向地理编码（中文地名），优先显示区/县级 `name` | 首页天气组件 |
| 8 | index.html | 加载 `lib/lunar.min.js` 黄历库，替换硬编码农历日期字符串 | 全站农历显示 |
| 9 | 构建日志-页面导航结构.md | 新增页面导航结构文档，记录全部 19 页的跳转关系和状态 | 文档 |
| 10 | index.html | 天气从随机数组改为 `navigator.geolocation` 定位 + Nominatim 反向地理编码 + Open-Meteo 实时温/码 | 首页天气组件 |
| 11 | index.html | 运签从 `Math.random()` 改为 `Lunar.fromDate().getDayInGanZhi()` 实时 60 甲子签，幸运色从天干5色改为地支12中国传统色 | 首页运签 |
| 12 | 全部 19 页 | 恢复 `font-weight:500`（用户确认要细体，已删掉 500 恢复默认 400 细体） | 全局字体 |
| 13 | timeline.html | 整体重写：从 V2 卡片式改为线分隔列表式，新增粘性顶部栏（←回首页 / 今日动态☁️ / 日历·编辑·＋），分类标签横向滚动（全部/出去玩/美食/日常/摄影/旅行/随笔），日历筛选（原生 date picker，可与标签叠加），新增弹窗（多选活动标签 + 单选心情 + 多图上传 base64），编辑资料弹窗（头像上传 + 显示名称），用户资料持久化存储，空状态提示，支持旧数据兼容格式 | 今日动态页 |
| 14 | index.html | 按照用户五层设计理念完全重写首页结构：第一层页眉（KANSHI简纸 + 日期 + 农历 + 天气行内标记 + 搜索折叠为🔍图标），第二层今日签（主签文占七成视觉权重 + 幸运色/宜/忌辅助条），第三层四个动作入口改为不对称拼贴（记一刻大主卡 + 记饮食/记一笔两列 + 存灵感虚线纸条），第四层今天的痕迹（跨动态/饮食/日记/灵感查询当天记录，空状态显示"今天还是一张空白的纸"），第五层手边工具（胶囊按钮横向排列） | 首页全页重构 |
| 15 | index.html | 第三层入口文字从功能名全部改为动作名（今日动态→记一刻、热量日记→记饮食、心灵日记→记一笔、灵感书架→存灵感），首页搜索从常驻大框改为🔍图标点击展开 | 首页交互 |
| 16 | index.html | 新增快速记账功能：点击"记一笔"弹出模态框，支持支出/收入切换、¥金额输入（32px大字体）、8分类网格（餐饮/购物/交通/生活/娱乐/学习/健康/其他）、备注输入，十秒完成一笔记录 | 首页第三层 |
| 17 | shared.js | 新增 `financeLogs: []` 字段到数据库结构，支撑快速记账数据持久化 | 数据层 |
| 18 | index.html | 第四层"今天的痕迹"新增跨数据源查询：同时读取当天动态、饮食日志、日记、灵感和记账，最多显示4条按时间倒序 | 首页第四层 |
| 19 | index.html | 排查并补充首页所有卡片的纸张质感：统一 `box-shadow:0 4px 10px rgba(28,27,26,0.08)` 阴影、`border-radius:20px` 圆角、`:active` 时 `scale(0.98)` 按压反馈和背景色加深 | 首页全局样式 |
| 20 | index.html | 天气组件从文字行内改为纸色胶囊按钮圆形样式（`border:1px solid #EAE7E1` + `background:#FBFBFA` + 按压反馈），与原版天气悬浮卡片更搭 | 首页第一层 |
| 21 | index_v2.html | 首页重构前备份当前版本为 `index_v2.html`，用户不满意可随时恢复 | 备份 |
| 22 | index.html | 将第三层四个入口卡片的图标从 emoji 替换为用户提供的 PNG 图标（记一刻→pic/动态.png、记饮食→pic/线路-饮食.png、记一笔→pic/174_记账.png、存灵感→pic/灵感.png），搜索按钮从 🔍 emoji 替换为 pic/放大镜.png | 首页图标 |
| 23 | index.html | 第三层入口从三种不同版式（大卡片+双列+横条）改为统一 2×2 网格 `.action-grid`，四张卡相同宽度/高度/内边距/图标尺寸/文字层级 | 首页第三层 |
| 24 | index.html | 今日签压缩纵向面积约15%：内边距 24→18px、主签文字号 20→17px、辅助信息间距和行距同步缩小，减少第一屏占用 | 首页第二层 |
| 25 | index.html | 空状态"今天的痕迹"去掉大白卡片背景，改为纯文字轻量提示；有记录时保留纸卡片，圆角从14px降至12px | 首页第四层 |
| 26 | index.html | 底部导航缩矮约20%：胶囊高度 64→52px、图标包裹圈 36→28px、导航按钮 48→40px、图标 20→16px、标签字号 8→7px | 首页导航 |
| 27 | index.html | 页眉精修：日期从三行收为两层（日期+星期一行，农历+定位+天气一行），搜索图标从 PNG 换为浅灰文字"搜索"，运签整体压缩 padding 再减 15%（14→12px），运签底部信息从单行三栏改三行垂直排列（幸运色/宜/忌各一行），签文每句后自动 `<br>` 断行展示，日干支标签移到卡片右上角 | 首页视觉精修 |

### 待办/风险项

- [ ] `calories.html` 原版仍处于损坏状态（所有 class = back-btn），需要确认是否用 `calories_v2.html` 覆盖
- [ ] `gallery.html` / `storage.html` 仍然维持 V2 样式，用户反馈视觉不满意
- [ ] Open-Meteo 天气 API 需 HTTPS，localhost 需允许地理定位权限
- [ ] 12 地支幸运色的 CSS dot 类已补充（zihei/tuhuang/songlv/ouhe/zhusha/yanzhi/tubai/yuebai/liujin/tuose/daizi）
- [ ] Nominatim 反向地理编码免费但有限频（1 req/s），定位刷新太快可能短时被限
